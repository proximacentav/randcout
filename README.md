# randcout
A simple terminal utility for random output to the terminal.
# Install 
1. download randcout.c
2. to compile use gcc -O2 -o randcout randcout.c
3. sudo cp randcout /usr/local/bin/ 
4.sudo chmod +x /usr/local/bin/randcout

# use
